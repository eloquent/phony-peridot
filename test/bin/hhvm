#!/bin/sh
docker run -it --rm \
    -v "${PWD}:/usr/src/phony-peridot" \
    -w /usr/src/phony-peridot \
    -e "PATH=vendor/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin" \
    hhvm/hhvm "$@"
